## 统一数据访问层 —— 全部走 Service

### 目标
消除项目中 ~53% 直接调用 DAO 的代码，统一改为通过 Service 层访问数据库。

### 变更文件总计：约 27 个

---

### 阶段 1：扩充 6 个现有 Service（添加缺失方法）

1. **GameTimeService** — 添加 `addTime()`, `updateTime()`, `deleteTimeById()`, `deleteTimeByData()`, `deleteTimeByDataAndRoleId()`, `getTimeListByData()`, `getTimeListByDataAndRoleId()`
2. **GameRecordService** — 添加 `addOrUpdateRecord()`
3. **TowerDataService** — 添加 `getEndTimeListByRoleId()`, `getRecordsByRoleIdAndEndTime()`
4. **SlashDataService** — 添加 `getEndTimesByRoleId()`, `getByRoleIdAndEndTime()`
5. **GameNewTowerService** — 添加 `getEndTimesByRoleId()`, `getByRoleIdAndEndTime()`
6. **SignService** — 添加 `getHistoriesByRoleId()`

### 阶段 2：创建 2 个新 Service

7. **GameSettingService** — 包装 `GameSettingDao`（操作游戏自身 SQLite）
8. **GameRoleDataService** — 包装 `GameRoleDataDao`

### 阶段 3：更新 18 个调用方（DAO 注入 → Service 注入）

**ViewModel（13个）：**
9. `GameTimeViewModel` — `GameTimeDao`→`GameTimeService`, `UserInfoDao`→`UserInfoService`
10. `GameRecordViewModel` — `GameRecordDao`→`GameRecordService`, `UserInfoDao`→`UserInfoService`
11. `HomeViewModel` — `GameTimeDao`→`GameTimeService`
12. `MainViewModel` — `UserInfoDao`→`UserInfoService`
13. `SignViewModel` — `UserInfoDao`→`UserInfoService`, `SignHistoryDao`→`SignService`
14. `TowerViewModel` — `GameTowerDataDao`→`TowerDataService`, `UserInfoDao`→`UserInfoService`
15. `SlashViewModel` — `GameSlashDataDao`→`SlashDataService`, `UserInfoDao`→`UserInfoService`
16. `NewTowerViewModel` — `GameNewTowerDao`→`GameNewTowerService`, `UserInfoDao`→`UserInfoService`
17. `TowerGroupViewModel` — `UserInfoDao`→`UserInfoService`
18. `CalculatorViewModel` — `UserInfoDao`→`UserInfoService`
19. `CalculatorRoleEditViewModel` — `UserInfoDao`→`UserInfoService`
20. `CalculatorWeaponEditViewModel` — `UserInfoDao`→`UserInfoService`
21. `ResourceBriefingViewModel` — `UserInfoDao`→`UserInfoService`
22. `OwnRoleViewModel` — `UserInfoDao`→`UserInfoService`

**View（2个）：**
23. `GameTimeAddView` — `AppInjector.getInstance(GameTimeDao)`→`GameTimeService`
24. `PlayTimeAlertItemView` — `AppInjector.getInstance(GameTimeDao)`→`GameTimeService`

**Task（2个）：**
25. `GameLogFileAnalysisTask` — `GameRecordDao`/`GameTimeDao`→Service
26. `NewGameLogFileAnalysisTask` — 同上

**其他（1个）：**
27. `GameAdvanceSettingViewModel` — `new GameSettingDao()`→`@Inject GameSettingService`

### 阶段 4：注册新 Service

28. `AppModule.java` — 添加 `GameSettingService` 和 `GameRoleDataService` 绑定

### 不做变更
- `VersionUpdateUtil`（DDL 迁移，基础设施代码）
- Service 内部调用 DAO（Service 是 DAO 的唯一消费者，合理）
- `GameSettingDao` 本身（操作游戏数据库，保持特殊实现）